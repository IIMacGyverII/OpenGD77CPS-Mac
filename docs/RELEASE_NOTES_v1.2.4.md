# OpenGD77 CPS — PriInterPhone fork v1.2.4

**Date:** June 5, 2026

## New

- **Pre-import validation** (`AndroidBackupValidator`) — Path B `_id` header check, relay=0 and duplicate channel name warnings; blocks bad folders before import.
- **Android backup manager** — validation log panel; import disabled when validation errors.
- **File → Import** — shows validation summary in confirm dialog.
- **Channel editor Ctrl+Z** — reverts channel + CSV-only fields (encrypt key, lat/lon, use location) to state when the editor was opened.

## Version

`FORK_VERSION` = **1.2.4**