# OpenGD77 CPS — PriInterPhone fork v2.0.2

**Date:** June 7, 2026

**Build:** `OpenGD77CPS-Mac_Build_20260607_162000.zip`

## Filter counts everywhere

Extends v2.0.0–2.0.1 grid filter counts to remaining fork filters:

- **Navigation tree** — `Filter (visible/total):` while searching zones, channels, contacts, etc.
- **TG / Rx group lists grid** — match count on filter
- **Scan lists grid** — match count on filter
- **TG/Rx list editor** — Available list filter count
- **Scan list editor** — Available list filter count

`FORK_VERSION` = **2.0.2**