# OpenGD77 CPS — PriInterPhone fork v1.4.4

**Date:** June 5, 2026

## Channel grid ↔ editor sync

- **Single-click** a row when the channel editor is already open → top pane loads that channel (master/detail).
- **Double-click** or **F2** still opens/switches the editor (v1.4.3).
- Hint label on Channels grid: *Click row to load in editor (when open); double-click or F2 to edit*.
- Welcome dialog release-notes link points to the current GitHub release tag.

`FORK_VERSION` = **1.4.4**