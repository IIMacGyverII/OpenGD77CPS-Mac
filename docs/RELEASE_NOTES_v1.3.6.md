# OpenGD77 CPS — PriInterPhone fork v1.3.6

**Date:** June 5, 2026

## Tier 2.4 — Import/export feedback

- **Scrollable summary dialog** after Path B batch import or export: files processed, per-file results, contact/channel/zone counts, and aggregated warnings in one view (replaces cramped `MessageBox` text).
- **Busy overlay** with marquee progress while CSV files import/export — UI stays responsive during multi-file operations.
- **Post-import warnings** include relay=0 coercion notes, duplicate channel names, and contact integrity findings from the backup folder.

`FORK_VERSION` = **1.3.6**