# OpenGD77 CPS — PriInterPhone fork v1.9.26

**Date:** June 7, 2026

**Build:** `OpenGD77CPS-Mac_Build_20260607_153000.zip`

## Studio launch + health status

- **Folder on exe** — drag a backup folder onto `OpenGD77CPS.exe` (or pass a folder path as the only argument) to open Codeplug Studio directly
- **--studio no flash** — main CPS window stays hidden until you choose **Open full editor…**
- **Studio title** — window title shows the loaded backup folder name
- **Standalone badge** — `--studio` header notes standalone mode
- **Health status bar** — shows `no zn:` count when channels are not in any zone

`FORK_VERSION` = **1.9.26**