# OpenGD77 CPS — PriInterPhone fork v2.0.0

**Date:** June 7, 2026

**Build:** `OpenGD77CPS-Mac_Build_20260607_160000.zip`

## Tier 3.2 — thin Codeplug Studio (MAJOR)

**Codeplug Studio** is the primary CSV-only workflow surface (v1.9.16–1.9.27). v2.0.0 packages it as a first-class launcher:

- **`CodeplugStudio.cmd`** — double-click to open Studio (`--studio`); pass or drag a backup folder path
- Full CPS remains via **Open full editor…** or normal `OpenGD77CPS.exe`
- **Channels grid** — filter label shows match count (`Filter (12/1024):`)
- Status bar hint mentions `CodeplugStudio.cmd`

Prior Studio features (unchanged): F8 parity, diff review, WebView2 reports, Desktop shortcut, folder drag-drop, health (F7).

`FORK_VERSION` = **2.0.0**