# OpenGD77 CPS — PriInterPhone fork v1.4.2

**Date:** June 5, 2026

## Tier 2.3 — Docking UX

- **First-run layout:** Opens Channels list + first channel, tiled horizontally (tree docked left).
- **Restore layout:** View → *Restore PriInterPhone default layout…* resets dock from `DockPanel.config.default` and reapplies the MDI workspace.
- **Persist layout:** Saves `DockPanel.config` on exit (dock pane positions).

`FORK_VERSION` = **1.4.2**