# OpenGD77 CPS — PriInterPhone fork v1.2.6

**Date:** June 5, 2026

## Fixes

- **Channel editor labels** — shell theme no longer forces light `ForeColor` on MDI children; labels restored on all docked editors when opened.
- **Android section note** — blue CSV hint text preserved after theme pass.

## New

- **Channel list filter** — name filter box on Channels grid.
- **Grid polish** — alternating row shading, double-buffered paint.
- **Import preview** — validation reports CSV channel row count vs loaded codeplug before import.

## Version

`FORK_VERSION` = **1.2.6**