# OpenGD77 CPS — PriInterPhone fork v1.9.9

**Date:** June 7, 2026

**Build:** `OpenGD77CPS-Mac_Build_20260607_124504.zip`

## Codeplug Studio — export UX + `--studio` launch

- **Export confirm** — Studio asks before overwriting CSVs in the loaded folder
- **In-form status** — validation report chip shows **Exported ✓** or **Import complete ✓** (errors open a details dialog parented to Studio, not behind it)
- **`--studio` launch** — Studio gets its own taskbar entry; main CPS window stays visible (no minimize/hide)

`FORK_VERSION` = **1.9.9**